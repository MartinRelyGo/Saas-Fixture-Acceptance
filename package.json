{
  "name": "ledgerly",
  "version": "0.1.0",
  "private": true,
  "description": "Disposable database-backed multi-tenant SaaS fixture used for AI Builder acceptance tests. Real Postgres (PGlite) with migrations and row-level security.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.2.17"
  }
}
